# 🎵 Speech Commands Classification API

Проект для класифікації аудіо команд (yes, no, up, down) з використанням CNN моделі на PyTorch та Flask API.

## 📋 Особливості

- ✅ **Багатоступенева Docker збірка** (multi-stage build)
- ✅ Автоматичне навчання моделі під час збірки (якщо модель відсутня)
- ✅ Оптимізований production-ready образ
- ✅ REST API з Flask
- ✅ Веб-інтерфейс для тестування
- ✅ Health checks та моніторинг
- ✅ Підтримка різних аудіо форматів (WAV, MP3, FLAC, OGG, WebM)

## 🐳 Швидкий старт з Docker

### ⚡ Важлива зміна архітектури

Модель **навчається при першому запуску контейнера**, а не під час збірки. Це вирішує проблеми з обмеженням пам'яті під час Docker build.

### Варіант 1: Docker Compose (рекомендовано)

```bash
# Збудувати образ (швидко, ~2-3 хв, БЕЗ навчання)
docker-compose build

# Запустити контейнер (модель навчиться при першому запуску ~5-10 хв)
docker-compose up -d

# Переглянути логи навчання
docker-compose logs -f speech-app

# Зупинити
docker-compose down
```

### Варіант 2: Docker команди

```bash
# Збудувати образ
docker build -t speech-commands-app .

# Запустити контейнер (з volume для збереження моделі)
docker run -d -p 8000:8000 \
  -v speech-model:/app \
  --name speech-app \
  speech-commands-app

# Переглянути логи
docker logs -f speech-app
```

### Варіант 3: З готовою моделлю (найшвидше)

```bash
# Якщо у вас вже є навчена модель локально
docker build -t speech-commands-app .

docker run -d -p 8000:8000 \
  -v $(pwd)/model_state_dict.pt:/app/model_state_dict.pt:ro \
  --name speech-app \
  speech-commands-app

# API запуститься одразу без навчання!
```

📚 **Детальна документація**: [RUNTIME_TRAINING.md](RUNTIME_TRAINING.md)

## 🏗️ Архітектура багатоступеневої збірки

Dockerfile складається з 3 етапів:

1. **Builder**: Встановлення всіх залежностей та копіювання коду
2. **Trainer**: Перевірка наявності моделі та навчання (якщо потрібно)
3. **Runtime**: Легкий фінальний образ для production

### Як працює автоматичне навчання

```
┌─────────────────────────────────────┐
│  Етап BUILDER                       │
│  - Встановлення залежностей         │
│  - Копіювання коду                  │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Етап TRAINER                       │
│  ┌────────────────────────────┐    │
│  │ Чи є model_state_dict.pt?  │    │
│  └────────┬──────────┬────────┘    │
│           │ НІ       │ ТАК          │
│           ▼          ▼              │
│  ┌────────────┐  ┌──────────┐      │
│  │ Навчання   │  │ Пропуск  │      │
│  │ моделі     │  │          │      │
│  └────────────┘  └──────────┘      │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Етап RUNTIME                       │
│  - Копіювання моделі                │
│  - Легкий образ без build tools     │
│  - Запуск Flask API                 │
└─────────────────────────────────────┘
```

## 🖥️ Локальний запуск (без Docker)

### 1. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 2. Навчання моделі (опціонально)

```bash
python speech_commands_train.py
```

### 3. Запуск API

```bash
python app.py
```

API буде доступний на http://localhost:8000

## 📡 API Endpoints

### GET /
Веб-інтерфейс для інтерактивного тестування

### GET /health
Перевірка статусу сервісу

```bash
curl http://localhost:8000/health
```

Відповідь:
```json
{
  "status": "healthy",
  "model_loaded": true,
  "device": "cpu"
}
```

### GET /info
Інформація про модель

```bash
curl http://localhost:8000/info
```

Відповідь:
```json
{
  "model": "SmallCNN",
  "classes": ["yes", "no", "up", "down"],
  "n_classes": 4,
  "device": "cpu",
  "parameters": 1234567
}
```

### POST /predict
Класифікація аудіо файлу

```bash
curl -X POST -F "file=@your_audio.wav" http://localhost:8000/predict
```

Відповідь:
```json
{
  "predicted": "yes",
  "confidence": 0.95,
  "probabilities": {
    "yes": 0.95,
    "no": 0.03,
    "up": 0.01,
    "down": 0.01
  },
  "filename": "your_audio.wav",
  "timestamp": 1699123456.78
}
```

## 🧪 Тестування

### Використання веб-інтерфейсу
1. Відкрийте http://localhost:8000 у браузері
2. Натисніть "Choose File" і виберіть аудіо файл
3. Натисніть "Classify"
4. Результати з'являться на сторінці

### Тестування з curl

```bash
# Health check
curl http://localhost:8000/health

# Інформація про модель
curl http://localhost:8000/info

# Класифікація аудіо
curl -X POST -F "file=@test.wav" http://localhost:8000/predict
```

## 📊 Структура проекту

```
Lab2/
├── Dockerfile                 # Багатоступенева збірка
├── docker-compose.yml         # Docker Compose конфігурація
├── .dockerignore             # Ігноровані файли для Docker
├── requirements.txt          # Python залежності
├── app.py                    # Flask API сервер
├── model_utils.py            # Утиліти для моделі
├── speech_commands_train.py  # Скрипт навчання моделі (4 класи для Docker)
├── test_docker_build.sh      # Тестовий скрипт
├── DOCKER_BUILD_GUIDE.md     # Детальна документація
├── DOCKER_FIXES.md           # Виправлення проблем Docker
├── templates/
│   └── index.html           # Веб-інтерфейс
├── data_speech/             # Датасет (завантажується автоматично)
└── model_state_dict.pt      # Навчена модель (створюється)
```

⚠️ **Важливо**: Для Docker збірки використовується **4 класи** (`yes`, `no`, `up`, `down`) 
для швидкого та стабільного навчання. Для локального навчання на всіх 34 класах 
див. `DOCKER_FIXES.md`.

## 🔧 Налаштування

### Зміна класів для розпізнавання

У файлах `app.py` та `speech_commands_train.py` змініть:

```python
CLASSES = ["yes", "no", "up", "down"]  # Ваші класи
```

### Параметри навчання

У `speech_commands_train.py`:

```python
BATCH_SIZE = 64    # Розмір батчу
EPOCHS = 10        # Кількість епох
LR = 1e-3          # Learning rate
```

## 🚀 Production deployment

### Використання з обмеженням ресурсів

```bash
docker run -d -p 8000:8000 \
    --memory="2g" \
    --cpus="2" \
    --restart unless-stopped \
    --name speech-app \
    speech-commands-app
```

### Збереження моделі поза контейнером

```bash
# Копіювати модель з контейнера
docker cp speech-app:/app/model_state_dict.pt ./

# Використовувати volume для моделі
docker run -d -p 8000:8000 \
    -v $(pwd)/model_state_dict.pt:/app/model_state_dict.pt:ro \
    --name speech-app \
    speech-commands-app
```

## 📚 Додаткова документація

- [DOCKER_BUILD_GUIDE.md](DOCKER_BUILD_GUIDE.md) - Детальна документація по Docker збірці
- [templates/index.html](templates/index.html) - Веб-інтерфейс

## 🎓 Вимоги

### Системні
- Docker 20.10+
- Docker Compose 2.0+ (опціонально)
- 4GB+ RAM для навчання моделі
- 3-4GB вільного місця на диску

### Python (для локального запуску)
- Python 3.10+
- PyTorch 2.1.0+
- Flask 2.0+
- torchaudio 2.1.0+

Повний список у [requirements.txt](requirements.txt)

## 🐛 Troubleshooting

### Проблема: Модель не навчається під час збірки

```bash
# Перевірте логи збірки
docker build -t speech-commands-app . 2>&1 | tee build.log

# Збудуйте тільки trainer етап
docker build --target trainer -t speech-trainer .
```

### Проблема: Контейнер не стартує

```bash
# Перегляньте логи
docker logs speech-app

# Запустіть в інтерактивному режимі
docker run -it --rm -p 8000:8000 speech-commands-app
```

### Проблема: Health check не проходить

```bash
# Перевірте чи контейнер запущений
docker ps

# Перевірте порт
curl http://localhost:8000/health

# Подивіться детальні логи
docker logs -f speech-app
```

## 📝 License

Цей проект використовує датасет Speech Commands v0.02, який ліцензовано під Creative Commons BY 4.0.

## 👥 Автор

Лабораторна робота з інтеграції ML-моделей

---

**Готово до запуску однією командою:**
```bash
docker-compose up -d && echo "🎉 API запущено на http://localhost:8000"
```
