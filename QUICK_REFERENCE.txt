╔══════════════════════════════════════════════════════════════════╗
║  🐳 ШВИДКИЙ ДОВІДНИК - Docker Multi-Stage Build                 ║
║  Speech Commands Classification API                             ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────┐
│ 🚀 ШВИДКИЙ СТАРТ                                                │
└──────────────────────────────────────────────────────────────────┘

  Найпростіший спосіб (Docker Compose):
  
    docker-compose up -d
  
  Альтернатива (прямі Docker команди):
  
    docker build -t speech-commands-app .
    docker run -d -p 8000:8000 --name speech-app speech-commands-app

┌──────────────────────────────────────────────────────────────────┐
│ 📦 ЗБІРКА ОБРАЗУ                                                 │
└──────────────────────────────────────────────────────────────────┘

  Звичайна збірка:
    docker build -t speech-commands-app .

  Без кешу:
    docker build --no-cache -t speech-commands-app .

  Тільки конкретний етап:
    docker build --target builder -t speech-builder .
    docker build --target trainer -t speech-trainer .
    docker build --target runtime -t speech-commands-app .

┌──────────────────────────────────────────────────────────────────┐
│ 🏃 ЗАПУСК КОНТЕЙНЕРА                                            │
└──────────────────────────────────────────────────────────────────┘

  Базовий запуск:
    docker run -d -p 8000:8000 --name speech-app speech-commands-app

  З restart policy:
    docker run -d -p 8000:8000 --restart unless-stopped \
      --name speech-app speech-commands-app

  З обмеженням ресурсів:
    docker run -d -p 8000:8000 --memory="2g" --cpus="2" \
      --name speech-app speech-commands-app

  Інтерактивний режим (для дебагу):
    docker run -it --rm -p 8000:8000 speech-commands-app

  З volume для моделі:
    docker run -d -p 8000:8000 \
      -v $(pwd)/model_state_dict.pt:/app/model_state_dict.pt:ro \
      --name speech-app speech-commands-app

┌──────────────────────────────────────────────────────────────────┐
│ 🔍 МОНІТОРИНГ І ЛОГИ                                            │
└──────────────────────────────────────────────────────────────────┘

  Логи в реальному часі:
    docker logs -f speech-app

  Останні 100 рядків:
    docker logs --tail 100 speech-app

  Статус контейнера:
    docker ps
    docker stats speech-app

  Детальна інформація:
    docker inspect speech-app

┌──────────────────────────────────────────────────────────────────┐
│ 🧪 ТЕСТУВАННЯ API                                               │
└──────────────────────────────────────────────────────────────────┘

  Health check:
    curl http://localhost:8000/health

  Інформація про модель:
    curl http://localhost:8000/info

  Класифікація аудіо:
    curl -X POST -F "file=@your_audio.wav" \
      http://localhost:8000/predict

  Веб-інтерфейс:
    open http://localhost:8000        (macOS)
    xdg-open http://localhost:8000    (Linux)
    start http://localhost:8000       (Windows)

┌──────────────────────────────────────────────────────────────────┐
│ 🛑 ЗУПИНКА І ОЧИЩЕННЯ                                           │
└──────────────────────────────────────────────────────────────────┘

  Зупинити контейнер:
    docker stop speech-app

  Запустити знову:
    docker start speech-app

  Видалити контейнер:
    docker rm speech-app
    docker rm -f speech-app  (з примусовою зупинкою)

  Видалити образ:
    docker rmi speech-commands-app

  Видалити все разом:
    docker stop speech-app && docker rm speech-app
    docker rmi speech-commands-app

┌──────────────────────────────────────────────────────────────────┐
│ 🔧 DOCKER COMPOSE КОМАНДИ                                       │
└──────────────────────────────────────────────────────────────────┘

  Запустити:
    docker-compose up -d

  Зупинити:
    docker-compose down

  Перезапустити:
    docker-compose restart

  Логи:
    docker-compose logs -f

  Ребілд і перезапуск:
    docker-compose up -d --build

  Видалити з volumes:
    docker-compose down -v

┌──────────────────────────────────────────────────────────────────┐
│ 📊 ІНФОРМАЦІЯ ПРО ОБРАЗ                                         │
└──────────────────────────────────────────────────────────────────┘

  Список образів:
    docker images | grep speech

  Розмір образу:
    docker images speech-commands-app

  Історія образу (шари):
    docker history speech-commands-app

  Детальна інформація:
    docker image inspect speech-commands-app

┌──────────────────────────────────────────────────────────────────┐
│ 🐚 ВИКОНАННЯ КОМАНД В КОНТЕЙНЕРІ                                │
└──────────────────────────────────────────────────────────────────┘

  Bash shell:
    docker exec -it speech-app /bin/bash

  Python REPL:
    docker exec -it speech-app python

  Перевірка наявності моделі:
    docker exec speech-app ls -lh /app/model_state_dict.pt

  Виконання Python скрипта:
    docker exec speech-app python -c "import torch; print(torch.__version__)"

┌──────────────────────────────────────────────────────────────────┐
│ 📁 КОПІЮВАННЯ ФАЙЛІВ                                            │
└──────────────────────────────────────────────────────────────────┘

  З контейнера на хост:
    docker cp speech-app:/app/model_state_dict.pt ./

  З хоста в контейнер:
    docker cp model_state_dict.pt speech-app:/app/

┌──────────────────────────────────────────────────────────────────┐
│ 🧹 ОЧИЩЕННЯ СИСТЕМИ                                             │
└──────────────────────────────────────────────────────────────────┘

  Видалити зупинені контейнери:
    docker container prune

  Видалити невикористані образи:
    docker image prune

  Видалити невикористані volumes:
    docker volume prune

  Очистити все (обережно!):
    docker system prune -a

┌──────────────────────────────────────────────────────────────────┐
│ 🔍 ДЕБАГ БАГАТОСТУПЕНЕВОЇ ЗБІРКИ                                │
└──────────────────────────────────────────────────────────────────┘

  Збірка з виведенням прогресу:
    docker build --progress=plain -t speech-commands-app .

  Збірка тільки builder етапу:
    docker build --target builder -t debug-builder .
    docker run -it --rm debug-builder /bin/bash

  Збірка тільки trainer етапу:
    docker build --target trainer -t debug-trainer .
    docker run -it --rm debug-trainer /bin/bash

  Перевірка чи модель тренується:
    docker build --target trainer -t debug-trainer . 2>&1 | grep "Модель"

┌──────────────────────────────────────────────────────────────────┐
│ 📈 PRODUCTION DEPLOYMENT                                         │
└──────────────────────────────────────────────────────────────────┘

  Повний production запуск:
    docker run -d \
      -p 8000:8000 \
      --name speech-app \
      --restart unless-stopped \
      --memory="2g" \
      --cpus="2" \
      --health-cmd="python -c 'import urllib.request; urllib.request.urlopen(\"http://localhost:8000/health\")'" \
      --health-interval=30s \
      --health-timeout=5s \
      --health-retries=3 \
      speech-commands-app

  З Docker Compose (production):
    docker-compose -f docker-compose.yml up -d --scale speech-app=3

┌──────────────────────────────────────────────────────────────────┐
│ ⚡ АВТОМАТИЧНИЙ ТЕСТ                                            │
└──────────────────────────────────────────────────────────────────┘

  Запуск тестового скрипта:
    chmod +x test_docker_build.sh
    ./test_docker_build.sh

┌──────────────────────────────────────────────────────────────────┐
│ 📚 ДОКУМЕНТАЦІЯ                                                 │
└──────────────────────────────────────────────────────────────────┘

  README.md              - Основна документація
  DOCKER_BUILD_GUIDE.md  - Детальний гайд по Docker
  docker-compose.yml     - Конфігурація Docker Compose

╔══════════════════════════════════════════════════════════════════╗
║  🎯 НАЙЧАСТІШІ СЦЕНАРІЇ                                          ║
╚══════════════════════════════════════════════════════════════════╝

  1️⃣ Перший запуск (модель навчиться автоматично):
     docker build -t speech-commands-app .
     docker run -d -p 8000:8000 --name speech-app speech-commands-app

  2️⃣ Перезапуск з оновленим кодом:
     docker stop speech-app && docker rm speech-app
     docker build -t speech-commands-app .
     docker run -d -p 8000:8000 --name speech-app speech-commands-app

  3️⃣ Швидкий перезапуск:
     docker restart speech-app

  4️⃣ Дебаг проблем:
     docker logs -f speech-app
     docker exec -it speech-app /bin/bash

  5️⃣ Повне очищення і перезбірка:
     docker stop speech-app && docker rm speech-app
     docker rmi speech-commands-app
     docker build --no-cache -t speech-commands-app .
     docker run -d -p 8000:8000 --name speech-app speech-commands-app

╔══════════════════════════════════════════════════════════════════╗
║  ✅ ENDPOINTS                                                    ║
╚══════════════════════════════════════════════════════════════════╝

  🌐 Web UI:     http://localhost:8000/
  ❤️  Health:     http://localhost:8000/health
  ℹ️  Info:       http://localhost:8000/info
  🎵 Predict:    POST http://localhost:8000/predict

╔══════════════════════════════════════════════════════════════════╗
║  🎉 ГОТОВО!                                                      ║
╚══════════════════════════════════════════════════════════════════╝
